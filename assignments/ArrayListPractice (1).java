import java.util.ArrayList;

public class ArrayListPractice 
{
	public static void main(String[] args) 
	{
		ArrayList<Integer> a = new ArrayList<Integer>();
		ArrayList<Integer> b = new ArrayList<Integer>();		
		
		a.add(new Integer(1));
		a.add(new Integer(4));
		a.add(new Integer(9));
		a.add(new Integer(16));
		
		b.add(new Integer(9));
		b.add(new Integer(7));
		b.add(new Integer(4));
		b.add(new Integer(9));
		b.add(new Integer(11));

		
		//Used for testing purposes only......
		//System.out.println(a);
		//System.out.println(b);
	}

	/*
	 * Program #13:  Create the append method.
	 * Input:  Two ArrayList of Integers
	 * Output:  A single Arraylist of Integers
	 * that has the elements from the 2nd list
	 * appended to the end of the first list.
	 */
	
	/*
	 * Program #14:  Create the merge method.
	 * Input:  Two ArrayList of Integers
	 * Output:  A single Arraylist of Integers
	 * that has all the elements from each input 
	 * list, added by alternating numbers from each list.
	 */

	
	/*
	 * Program #15:  Create the mergeSorted method.
	 * Input:  Two ArrayList of Integers, 
	 * 			SORTED IN INCREASING ORDER
	 * Output:  A single Arraylist of Integers
	 * that has all the elements from each input 
	 * list, listed from least to greatest.
	 */
	
}
